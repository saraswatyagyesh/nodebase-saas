// import { requireAuth } from "@/lib/auth-utils"; // Import the authentication function

const Page = () => { // Make this function async for authentication purposes

    // await requireAuth(); 
    return <p>Workflows</p>;
};

export default Page;